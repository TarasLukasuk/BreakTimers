<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="RoundedGrid" TargetType="Border">
        <Setter Property="Width" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Height" Value="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Background" Value="{StaticResource TransparentColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius">
            <Setter.Value>
                <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}" Converter="{StaticResource PerfectRoundingConverter}" />
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="{StaticResource TransparentColor}" />
    </Style>

</ResourceDictionary>
